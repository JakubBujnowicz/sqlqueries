source("~/.Rprofile")

if (interactive()) {
    .pkgs <- c("devtools", "usethis", "roxygen2", "tictoc")
    invisible(lapply(.pkgs, library,
                     character.only = TRUE,
                     quietly = TRUE))
}
