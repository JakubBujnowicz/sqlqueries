sql_query_df <- function(from,
                         select = "*",
                         distinct = FALSE,
                         where = NULL,
                         group_by = NULL,
                         order_by = NULL,
                         having = NULL,
                         schema = NULL,
                         ...)
{
    # Assertions ---------------------------------------------------------------

    # Code ---------------------------------------------------------------------
    rslt <- paste0("SELECT")
}
